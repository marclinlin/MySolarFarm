<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.marcoslin.solarfinal">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-feature android:name="android.hardware.location.gps" />

    <permission
        android:name="com.pkg.map.permission.MAPS_RECEIVE"
        android:protectionLevel="signature" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_myicon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_myicon_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".app.Activities.P3Activity" />
        <activity android:name=".app.Activities.P4Activity" />
        <activity android:name=".app.Activities.P5Activity" />
        <activity android:name=".app.Activities.P2Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".app.Activities.P6Activity"
            android:label="@string/title_activity_p6" />
        <activity android:name=".app.Activities.P8Activity" />
        <activity android:name=".app.Activities.P9Activity" />
        <activity android:name=".app.Activities.P10Activity" />
        <activity android:name=".app.Activities.P11Activity" />
        <activity android:name=".app.Activities.P12Activity" />
        <activity android:name=".app.Activities.P13Activity" />
        <activity android:name=".app.Activities.P3Activity_a" />
        <activity android:name=".app.Activities.P7Activity" />
        <activity android:name=".app.Activities.P9Activity_a" />
        <activity android:name=".app.Activities.P14Activity" />
        <activity android:name=".app.Activities.P15Activity" />
        <activity
            android:name=".app.Activities.P9Activity_b"
            android:label="@string/title_activity_p9_b"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".app.Activities.P8Activity_a" />
        <activity android:name=".app.Activities.P12Activity_a" />
        <activity android:name=".app.Activities.P14Activity_a"></activity>
    </application>

</manifest>